# BootCamp

# CI — `pipeline` branch

This branch contains the **CI configuration** for CI process of building an application and pushing an image to a docker hub repository.<br>

**Note!** there is no testing phase here. Also this example can be universal for other parts of the project (not just the backend)

---

## Related Branches

| Branch | Contents |
|--------|---------|
| [`docker`](../../tree/docker) | Dockerfiles for backend and frontend |
| [`pipeline`](../../tree/pipeline) | ← **you are here** — GitHub Actions CI configuration |
| [`infra`](../../tree/infra) | Terraform AWS infrastructure |