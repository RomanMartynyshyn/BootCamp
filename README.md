# BootCamp

here is just an example of what a CI process of building an application and pushing an image to a docker hub repository might look like. there is no testing phase here.
Also this example can be universal for other parts of the project (not just the backend)